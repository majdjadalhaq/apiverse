# APIVerse Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build APIVerse — a fullstack SaaS where users browse public APIs, see live sandboxed demos, bookmark favorites, and submit/upvote community demos.

**Architecture:** Next.js 14 App Router + Supabase (auth + DB + realtime) + sandboxed iframe demo runner. Main app proxies API calls; demos run in isolated `<iframe sandbox>` communicating via `postMessage`. Community features layered on top of core explorer.

**Tech Stack:** Next.js 14, TypeScript, Supabase JS v2, Tailwind CSS v4, Framer Motion v11, GSAP v3, Lenis, shadcn/ui, Vitest, Playwright

---

## Git & PR Workflow

- Branch naming: `feat/<task-name>`, `fix/<issue>`, `chore/<task>`
- Commits: Conventional Commits (`feat:`, `fix:`, `chore:`, `docs:`, `test:`)
- Each task = 1 feature branch → 1 PR → merge to `main`
- PR title = short imperative ("Add auth with Supabase OAuth")
- PR body = What / Why / How / Test plan / Screenshots (if UI)
- `main` branch protected: require PR, no direct push

---

## File Map

```
apiverse/
├── app/
│   ├── (auth)/
│   │   ├── login/page.tsx
│   │   └── auth/callback/route.ts
│   ├── (main)/
│   │   ├── layout.tsx
│   │   ├── page.tsx                    # Hero / landing
│   │   ├── explore/
│   │   │   ├── page.tsx               # Browse all APIs
│   │   │   └── [category]/page.tsx    # Category filter
│   │   ├── api/[slug]/
│   │   │   └── page.tsx               # API detail + demo runner
│   │   ├── profile/
│   │   │   └── page.tsx
│   │   └── collections/
│   │       ├── page.tsx
│   │       └── [id]/page.tsx
│   └── api/
│       ├── proxy/route.ts             # Safe API proxy
│       └── seed/route.ts              # One-time seed endpoint
├── components/
│   ├── ui/                            # shadcn primitives
│   ├── demo/
│   │   ├── DemoRunner.tsx             # iframe wrapper + postMessage
│   │   ├── DemoControls.tsx           # inputs before running demo
│   │   └── demos/                     # one file per official demo
│   │       ├── JokeDemo.tsx
│   │       ├── WeatherDemo.tsx
│   │       └── ...
│   ├── api-card/
│   │   ├── ApiCard.tsx
│   │   └── ApiCardSkeleton.tsx
│   ├── explore/
│   │   ├── SearchBar.tsx
│   │   ├── CategoryFilter.tsx
│   │   └── ApiGrid.tsx
│   ├── hero/
│   │   ├── Hero.tsx
│   │   └── HeroCanvas.tsx             # R3F scene
│   └── layout/
│       ├── Navbar.tsx
│       └── Footer.tsx
├── lib/
│   ├── supabase/
│   │   ├── client.ts                  # browser client
│   │   ├── server.ts                  # server client (cookies)
│   │   └── middleware.ts
│   ├── api-data/
│   │   ├── apis.ts                    # static typed API list
│   │   └── categories.ts
│   ├── demo-sandbox/
│   │   └── messages.ts                # postMessage types
│   └── utils.ts
├── public/
│   └── sandbox/
│       └── runner.html                # isolated iframe page
├── supabase/
│   └── migrations/
│       ├── 001_initial_schema.sql
│       └── 002_rls_policies.sql
├── tests/
│   ├── unit/
│   └── e2e/
├── .env.local.example
├── middleware.ts
└── docs/
    └── superpowers/
        └── plans/
            └── 2026-04-21-apiverse.md
```

---

## Task 1: GitHub Repo + Project Init

**Branch:** `chore/project-setup`

**PR title:** `chore: initialize Next.js project with full toolchain`

**PR body:**
```
## What
Bootstrap APIVerse with Next.js 14, TypeScript, Tailwind v4, ESLint, Prettier, Vitest, Playwright.

## Why
Clean foundation with linting, formatting, and test infrastructure from day one prevents technical debt.

## Test plan
- [ ] `npm run dev` starts without errors
- [ ] `npm run lint` passes
- [ ] `npm test` runs (no tests yet, exits 0)
```

**Files:**
- Create: entire project scaffolding

- [ ] **Step 1: Create GitHub repo**
```bash
gh repo create apiverse --public --description "Browse, test, and explore public APIs with live sandboxed demos" --clone
cd apiverse
```

- [ ] **Step 2: Init Next.js**
```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"
```

- [ ] **Step 3: Install dependencies**
```bash
npm install framer-motion gsap @studio-freight/lenis @supabase/supabase-js @supabase/ssr
npm install @react-three/fiber @react-three/drei three
npm install lucide-react clsx tailwind-merge
npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom @playwright/test
npx shadcn@latest init
```

- [ ] **Step 4: Configure Vitest — create `vitest.config.ts`**
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    globals: true,
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, '.') },
  },
})
```

- [ ] **Step 5: Create `tests/setup.ts`**
```typescript
import '@testing-library/jest-dom'
```

- [ ] **Step 6: Create `.env.local.example`**
```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

- [ ] **Step 7: Add scripts to `package.json`**
```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "next lint",
  "test": "vitest run",
  "test:watch": "vitest",
  "test:e2e": "playwright test"
}
```

- [ ] **Step 8: Create `.gitignore` entry for `.env.local`**
Verify `.env.local` is in `.gitignore` (create-next-app adds it, double-check).

- [ ] **Step 9: Commit and push**
```bash
git add .
git commit -m "chore: initialize Next.js 14 project with TypeScript, Tailwind v4, Vitest, Playwright"
git push -u origin main
```

- [ ] **Step 10: Create PR**
```bash
git checkout -b chore/project-setup
# (already on main — for future tasks, branch before coding)
# For this init task, push main directly as initial commit
```

---

## Task 2: Supabase Setup + Database Schema

**Branch:** `feat/supabase-schema`

**PR title:** `feat: add Supabase project config and initial database schema`

**PR body:**
```
## What
- Supabase client helpers (browser + server)
- Database migrations: profiles, apis, demos, bookmarks, collections, comments, upvotes
- Row Level Security policies
- Next.js middleware for session refresh

## Why
Schema first. All future features depend on these tables.

## Test plan
- [ ] Migrations run without error in Supabase dashboard
- [ ] RLS policies block unauthenticated writes
- [ ] `npm run test` passes unit test for supabase client initialization
```

**Files:**
- Create: `lib/supabase/client.ts`, `lib/supabase/server.ts`, `lib/supabase/middleware.ts`
- Create: `middleware.ts`
- Create: `supabase/migrations/001_initial_schema.sql`
- Create: `supabase/migrations/002_rls_policies.sql`
- Create: `tests/unit/supabase-client.test.ts`

- [ ] **Step 1: Create Supabase project**
Go to supabase.com → New project → name: `apiverse` → save URL + anon key + service role key to `.env.local`

- [ ] **Step 2: Create `lib/supabase/client.ts`**
```typescript
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

- [ ] **Step 3: Create `lib/supabase/server.ts`**
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export function createClient() {
  const cookieStore = cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) =>
            cookieStore.set(name, value, options)
          )
        },
      },
    }
  )
}
```

- [ ] **Step 4: Create `middleware.ts` (root)**
```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  await supabase.auth.getUser()
  return supabaseResponse
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
}
```

- [ ] **Step 5: Create `supabase/migrations/001_initial_schema.sql`**
```sql
-- profiles
create table profiles (
  id uuid references auth.users on delete cascade primary key,
  username text unique not null,
  avatar_url text,
  bio text,
  created_at timestamptz default now() not null
);

-- apis (seeded from public-apis list)
create table apis (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  description text not null,
  category text not null,
  url text not null,
  auth text default 'No',
  https boolean default true,
  cors text default 'Unknown',
  slug text unique not null,
  created_at timestamptz default now() not null
);

-- demos
create table demos (
  id uuid primary key default gen_random_uuid(),
  api_id uuid references apis on delete cascade not null,
  title text not null,
  description text,
  author_id uuid references profiles on delete set null,
  upvotes_count integer default 0 not null,
  is_official boolean default false not null,
  created_at timestamptz default now() not null
);

-- bookmarks
create table bookmarks (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles on delete cascade not null,
  api_id uuid references apis on delete cascade not null,
  created_at timestamptz default now() not null,
  unique(user_id, api_id)
);

-- collections
create table collections (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles on delete cascade not null,
  name text not null,
  description text,
  is_public boolean default false not null,
  created_at timestamptz default now() not null
);

-- collection_apis
create table collection_apis (
  collection_id uuid references collections on delete cascade not null,
  api_id uuid references apis on delete cascade not null,
  primary key(collection_id, api_id)
);

-- comments
create table comments (
  id uuid primary key default gen_random_uuid(),
  demo_id uuid references demos on delete cascade not null,
  author_id uuid references profiles on delete cascade not null,
  content text not null,
  created_at timestamptz default now() not null
);

-- upvotes
create table upvotes (
  user_id uuid references profiles on delete cascade not null,
  demo_id uuid references demos on delete cascade not null,
  primary key(user_id, demo_id)
);

-- auto-create profile on signup
create or replace function handle_new_user()
returns trigger as $$
begin
  insert into profiles (id, username, avatar_url)
  values (
    new.id,
    coalesce(new.raw_user_meta_data->>'preferred_username', split_part(new.email, '@', 1)),
    new.raw_user_meta_data->>'avatar_url'
  );
  return new;
end;
$$ language plpgsql security definer;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute procedure handle_new_user();
```

- [ ] **Step 6: Create `supabase/migrations/002_rls_policies.sql`**
```sql
-- Enable RLS
alter table profiles enable row level security;
alter table apis enable row level security;
alter table demos enable row level security;
alter table bookmarks enable row level security;
alter table collections enable row level security;
alter table collection_apis enable row level security;
alter table comments enable row level security;
alter table upvotes enable row level security;

-- profiles: public read, own write
create policy "profiles_select_all" on profiles for select using (true);
create policy "profiles_update_own" on profiles for update using (auth.uid() = id);

-- apis: public read, no user write (seeded by service role)
create policy "apis_select_all" on apis for select using (true);

-- demos: public read, auth insert, own update/delete
create policy "demos_select_all" on demos for select using (true);
create policy "demos_insert_auth" on demos for insert with check (auth.uid() = author_id);
create policy "demos_update_own" on demos for update using (auth.uid() = author_id);
create policy "demos_delete_own" on demos for delete using (auth.uid() = author_id);

-- bookmarks: own only
create policy "bookmarks_select_own" on bookmarks for select using (auth.uid() = user_id);
create policy "bookmarks_insert_own" on bookmarks for insert with check (auth.uid() = user_id);
create policy "bookmarks_delete_own" on bookmarks for delete using (auth.uid() = user_id);

-- collections: public if public, own if private
create policy "collections_select" on collections for select
  using (is_public = true or auth.uid() = user_id);
create policy "collections_insert_own" on collections for insert with check (auth.uid() = user_id);
create policy "collections_update_own" on collections for update using (auth.uid() = user_id);
create policy "collections_delete_own" on collections for delete using (auth.uid() = user_id);

-- comments: public read, auth insert, own delete
create policy "comments_select_all" on comments for select using (true);
create policy "comments_insert_auth" on comments for insert with check (auth.uid() = author_id);
create policy "comments_delete_own" on comments for delete using (auth.uid() = author_id);

-- upvotes: public read, auth insert/delete own
create policy "upvotes_select_all" on upvotes for select using (true);
create policy "upvotes_insert_auth" on upvotes for insert with check (auth.uid() = user_id);
create policy "upvotes_delete_own" on upvotes for delete using (auth.uid() = user_id);
```

- [ ] **Step 7: Run migrations**
Supabase dashboard → SQL Editor → paste + run `001_initial_schema.sql`, then `002_rls_policies.sql`.

- [ ] **Step 8: Write unit test — `tests/unit/supabase-client.test.ts`**
```typescript
import { describe, it, expect, vi } from 'vitest'

vi.mock('@supabase/ssr', () => ({
  createBrowserClient: vi.fn(() => ({ auth: {}, from: vi.fn() })),
}))

describe('supabase browser client', () => {
  it('creates client without throwing', async () => {
    const { createClient } = await import('@/lib/supabase/client')
    expect(() => createClient()).not.toThrow()
  })
})
```

- [ ] **Step 9: Run test**
```bash
npm test
```
Expected: PASS

- [ ] **Step 10: Commit + PR**
```bash
git checkout -b feat/supabase-schema
git add .
git commit -m "feat: add Supabase client helpers, DB schema, and RLS policies"
git push -u origin feat/supabase-schema
gh pr create --title "feat: add Supabase project config and initial database schema" --body "$(cat <<'EOF'
## What
- Supabase browser/server client helpers using @supabase/ssr
- DB migrations: profiles, apis, demos, bookmarks, collections, comments, upvotes
- RLS policies: public read for apis/demos, authenticated writes, own-only for bookmarks
- Trigger: auto-create profile on new user signup
- Next.js middleware for session cookie refresh

## Why
Schema-first approach. All feature branches depend on these tables being in place.

## How
Run migrations manually in Supabase SQL Editor (001 then 002). Future: add Supabase CLI migration runner.

## Test plan
- [x] Unit test for browser client init passes
- [ ] Migrations run without error in dashboard
- [ ] RLS: anonymous user cannot insert into bookmarks (verify in dashboard table editor)
EOF
)"
```

---

## Task 3: Auth — GitHub + Google OAuth

**Branch:** `feat/auth`

**PR title:** `feat: add GitHub and Google OAuth with Supabase Auth`

**PR body:**
```
## What
- Login page with GitHub + Google OAuth buttons
- Auth callback route handler
- Protected route helper
- Navbar shows user avatar / login CTA

## Why
Auth gates bookmarks, collections, demo submissions — needed before any user feature.

## Test plan
- [ ] GitHub OAuth flow completes end-to-end
- [ ] User profile auto-created in DB (verify in Supabase)
- [ ] Visiting /profile while logged out redirects to /login
```

**Files:**
- Create: `app/(auth)/login/page.tsx`
- Create: `app/auth/callback/route.ts`
- Create: `lib/auth.ts`
- Modify: `components/layout/Navbar.tsx`

- [ ] **Step 1: Enable OAuth in Supabase**
Supabase dashboard → Auth → Providers → enable GitHub (add client ID + secret from github.com/settings/developers) → enable Google (add credentials from console.cloud.google.com).
Add to both: callback URL `http://localhost:3000/auth/callback` and your Vercel URL.

- [ ] **Step 2: Create `app/auth/callback/route.ts`**
```typescript
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  const next = searchParams.get('next') ?? '/'

  if (code) {
    const supabase = createClient()
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) return NextResponse.redirect(`${origin}${next}`)
  }

  return NextResponse.redirect(`${origin}/login?error=auth_failed`)
}
```

- [ ] **Step 3: Create `lib/auth.ts`**
```typescript
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'

export async function requireAuth() {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')
  return user
}

export async function getUser() {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  return user
}
```

- [ ] **Step 4: Create `app/(auth)/login/page.tsx`**
```typescript
'use client'
import { createClient } from '@/lib/supabase/client'
import { Github } from 'lucide-react'

export default function LoginPage() {
  const supabase = createClient()

  const signIn = (provider: 'github' | 'google') =>
    supabase.auth.signInWithOAuth({
      provider,
      options: { redirectTo: `${location.origin}/auth/callback` },
    })

  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="flex flex-col gap-4 w-full max-w-sm p-8">
        <h1 className="text-2xl font-bold text-center">Sign in to APIVerse</h1>
        <button
          onClick={() => signIn('github')}
          className="flex items-center justify-center gap-2 rounded-lg border px-4 py-2 hover:bg-neutral-100 dark:hover:bg-neutral-800 transition-colors"
        >
          <Github size={18} />
          Continue with GitHub
        </button>
        <button
          onClick={() => signIn('google')}
          className="flex items-center justify-center gap-2 rounded-lg border px-4 py-2 hover:bg-neutral-100 dark:hover:bg-neutral-800 transition-colors"
        >
          Continue with Google
        </button>
      </div>
    </main>
  )
}
```

- [ ] **Step 5: Write unit test — `tests/unit/auth.test.ts`**
```typescript
import { describe, it, expect, vi } from 'vitest'

vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(() => ({
    auth: { getUser: vi.fn().mockResolvedValue({ data: { user: null } }) },
  })),
}))
vi.mock('next/navigation', () => ({ redirect: vi.fn() }))

describe('requireAuth', () => {
  it('redirects to /login when no user', async () => {
    const { redirect } = await import('next/navigation')
    const { requireAuth } = await import('@/lib/auth')
    await requireAuth()
    expect(redirect).toHaveBeenCalledWith('/login')
  })
})
```

- [ ] **Step 6: Run tests**
```bash
npm test
```
Expected: PASS

- [ ] **Step 7: Commit + PR**
```bash
git checkout -b feat/auth
git add .
git commit -m "feat: add GitHub/Google OAuth login with Supabase Auth"
git push -u origin feat/auth
gh pr create --title "feat: add GitHub and Google OAuth with Supabase Auth" --body "$(cat <<'EOF'
## What
- /login page with GitHub + Google OAuth sign-in buttons
- /auth/callback route to exchange code for session
- requireAuth() server helper — redirects unauthenticated users
- getUser() server helper for optional auth checks

## Why
Auth is required before building bookmarks, collections, and demo submission.

## How
Uses @supabase/ssr with OAuth PKCE flow. Callback URL registered in both Supabase dashboard and OAuth provider apps.

## Test plan
- [x] requireAuth() redirects unauthenticated users (unit test)
- [ ] Manual: GitHub OAuth completes, profile auto-created in DB
- [ ] Manual: Google OAuth completes
- [ ] Manual: /profile redirect to /login when logged out
EOF
)"
```

---

## Task 4: API Data Layer + Seed

**Branch:** `feat/api-data-layer`

**PR title:** `feat: add static API data types and Supabase seed endpoint`

**PR body:**
```
## What
- TypeScript types for API entries
- Static data file with 200+ APIs from public-apis list
- /api/seed route (service-role protected) to populate DB once
- Category list with counts

## Why
The explore page and all features depend on APIs existing in the DB.

## Test plan
- [ ] Seed endpoint populates apis table (verify count in Supabase)
- [ ] Unit test: slug generation produces URL-safe strings
```

**Files:**
- Create: `lib/api-data/types.ts`
- Create: `lib/api-data/apis.ts`
- Create: `lib/api-data/categories.ts`
- Create: `lib/utils.ts`
- Create: `app/api/seed/route.ts`
- Create: `tests/unit/slug.test.ts`

- [ ] **Step 1: Create `lib/api-data/types.ts`**
```typescript
export type ApiAuth = 'apiKey' | 'OAuth' | 'No' | 'X-Mashape-Key' | 'User-Agent'
export type ApiCors = 'Yes' | 'No' | 'Unknown'

export interface PublicApi {
  name: string
  description: string
  category: string
  url: string
  auth: ApiAuth
  https: boolean
  cors: ApiCors
  slug: string
}
```

- [ ] **Step 2: Create `lib/utils.ts`**
```typescript
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

export function slugify(text: string): string {
  return text
    .toLowerCase()
    .replace(/[^\w\s-]/g, '')
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')
    .trim()
}
```

- [ ] **Step 3: Write slug test — `tests/unit/slug.test.ts`**
```typescript
import { describe, it, expect } from 'vitest'
import { slugify } from '@/lib/utils'

describe('slugify', () => {
  it('lowercases and replaces spaces with hyphens', () => {
    expect(slugify('Open Weather Map')).toBe('open-weather-map')
  })
  it('removes special characters', () => {
    expect(slugify('NASA (APOD)')).toBe('nasa-apod')
  })
  it('collapses multiple hyphens', () => {
    expect(slugify('Cat  Facts')).toBe('cat-facts')
  })
})
```

- [ ] **Step 4: Run test — verify it fails**
```bash
npm test tests/unit/slug.test.ts
```
Expected: FAIL — `slugify is not a function`

- [ ] **Step 5: Run test after creating utils.ts**
```bash
npm test tests/unit/slug.test.ts
```
Expected: PASS (3 tests)

- [ ] **Step 6: Create `lib/api-data/apis.ts`**
Seed data — first 30 entries (add more over time). Each entry follows `PublicApi` type:
```typescript
import { slugify } from '@/lib/utils'
import type { PublicApi } from './types'

export const PUBLIC_APIS: PublicApi[] = [
  {
    name: 'Cat Facts',
    description: 'Daily cat facts',
    category: 'Animals',
    url: 'https://alexwohlbruck.github.io/cat-facts/',
    auth: 'No',
    https: true,
    cors: 'No',
    slug: slugify('Cat Facts'),
  },
  {
    name: 'Dog CEO',
    description: 'Random dog images from the Dog CEO collection',
    category: 'Animals',
    url: 'https://dog.ceo/dog-api/',
    auth: 'No',
    https: true,
    cors: 'Yes',
    slug: slugify('Dog CEO'),
  },
  {
    name: 'Open Weather Map',
    description: 'Weather data and forecasts',
    category: 'Weather',
    url: 'https://openweathermap.org/api',
    auth: 'apiKey',
    https: true,
    cors: 'No',
    slug: slugify('Open Weather Map'),
  },
  {
    name: 'NASA APOD',
    description: 'Astronomy Picture of the Day',
    category: 'Science',
    url: 'https://api.nasa.gov/',
    auth: 'apiKey',
    https: true,
    cors: 'No',
    slug: slugify('NASA APOD'),
  },
  {
    name: 'JokeAPI',
    description: 'Programming and general jokes',
    category: 'Entertainment',
    url: 'https://sv443.net/jokeapi/v2/',
    auth: 'No',
    https: true,
    cors: 'Yes',
    slug: slugify('JokeAPI'),
  },
  {
    name: 'PokeAPI',
    description: 'Pokémon data: abilities, moves, species',
    category: 'Games',
    url: 'https://pokeapi.co/',
    auth: 'No',
    https: true,
    cors: 'Yes',
    slug: slugify('PokeAPI'),
  },
  {
    name: 'Open Trivia DB',
    description: 'Trivia questions across categories and difficulties',
    category: 'Entertainment',
    url: 'https://opentdb.com/api_config.php',
    auth: 'No',
    https: true,
    cors: 'Unknown',
    slug: slugify('Open Trivia DB'),
  },
  {
    name: 'The Movie DB',
    description: 'Movies, TV shows, actors data',
    category: 'Entertainment',
    url: 'https://www.themoviedb.org/documentation/api',
    auth: 'apiKey',
    https: true,
    cors: 'Unknown',
    slug: slugify('The Movie DB'),
  },
  {
    name: 'RandomUser',
    description: 'Random user/person data for prototyping',
    category: 'Test Data',
    url: 'https://randomuser.me/',
    auth: 'No',
    https: true,
    cors: 'Yes',
    slug: slugify('RandomUser'),
  },
  {
    name: 'REST Countries',
    description: 'Country data: name, flag, population, currency',
    category: 'Geography',
    url: 'https://restcountries.com/',
    auth: 'No',
    https: true,
    cors: 'Yes',
    slug: slugify('REST Countries'),
  },
]
```

- [ ] **Step 7: Create `lib/api-data/categories.ts`**
```typescript
import { PUBLIC_APIS } from './apis'

export const CATEGORIES = [...new Set(PUBLIC_APIS.map(api => api.category))].sort()

export function getApisByCategory(category: string) {
  return PUBLIC_APIS.filter(api => api.category === category)
}
```

- [ ] **Step 8: Create seed route — `app/api/seed/route.ts`**
```typescript
import { createClient } from '@supabase/supabase-js'
import { PUBLIC_APIS } from '@/lib/api-data/apis'
import { NextResponse } from 'next/server'

export async function POST(request: Request) {
  const authHeader = request.headers.get('authorization')
  if (authHeader !== `Bearer ${process.env.SUPABASE_SERVICE_ROLE_KEY}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  const supabase = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  )

  const { error } = await supabase.from('apis').upsert(PUBLIC_APIS, { onConflict: 'slug' })
  if (error) return NextResponse.json({ error: error.message }, { status: 500 })

  return NextResponse.json({ seeded: PUBLIC_APIS.length })
}
```

- [ ] **Step 9: Run seed**
```bash
curl -X POST http://localhost:3000/api/seed \
  -H "Authorization: Bearer $(grep SUPABASE_SERVICE_ROLE_KEY .env.local | cut -d= -f2)"
```
Expected: `{"seeded":10}` (or however many you have)

- [ ] **Step 10: Commit + PR**
```bash
git checkout -b feat/api-data-layer
git add .
git commit -m "feat: add API data types, static seed data, and seed endpoint"
git push -u origin feat/api-data-layer
gh pr create --title "feat: add static API data types and Supabase seed endpoint" --body "$(cat <<'EOF'
## What
- PublicApi TypeScript type
- slugify() utility with unit tests
- Static list of 10+ curated public APIs (expandable)
- /api/seed POST endpoint (service-role protected) for DB population
- Category helpers

## Why
All explore/browse features need real API data in the DB. Seed runs once, data persists.

## How
Seed endpoint uses Supabase service role key (not exposed to client). Upsert on slug prevents duplicates on re-seed.

## Test plan
- [x] slugify() unit tests pass (3 cases)
- [ ] Seed endpoint returns 200 with count
- [ ] Supabase apis table shows correct row count
EOF
)"
```

---

## Task 5: Explore Page — Browse + Filter + Search

**Branch:** `feat/explore-page`

**PR title:** `feat: add API explorer with category filters and search`

**PR body:**
```
## What
- /explore page listing all APIs from DB
- Category sidebar filter
- Text search (client-side)
- ApiCard component with category badge, auth indicator, HTTPS badge
- Loading skeletons
- Framer Motion stagger animation on card grid

## Test plan
- [ ] ApiCard renders name, description, category
- [ ] Search filters cards in real time
- [ ] Category filter shows correct subset
```

**Files:**
- Create: `components/api-card/ApiCard.tsx`
- Create: `components/api-card/ApiCardSkeleton.tsx`
- Create: `components/explore/SearchBar.tsx`
- Create: `components/explore/CategoryFilter.tsx`
- Create: `components/explore/ApiGrid.tsx`
- Create: `app/(main)/explore/page.tsx`
- Create: `tests/unit/ApiCard.test.tsx`

- [ ] **Step 1: Write failing test — `tests/unit/ApiCard.test.tsx`**
```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import { ApiCard } from '@/components/api-card/ApiCard'

const mockApi = {
  id: '1',
  name: 'Dog CEO',
  description: 'Random dog images',
  category: 'Animals',
  url: 'https://dog.ceo',
  auth: 'No',
  https: true,
  cors: 'Yes',
  slug: 'dog-ceo',
  created_at: '',
}

describe('ApiCard', () => {
  it('renders API name and description', () => {
    render(<ApiCard api={mockApi} />)
    expect(screen.getByText('Dog CEO')).toBeInTheDocument()
    expect(screen.getByText('Random dog images')).toBeInTheDocument()
  })

  it('renders category badge', () => {
    render(<ApiCard api={mockApi} />)
    expect(screen.getByText('Animals')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run test — verify FAIL**
```bash
npm test tests/unit/ApiCard.test.tsx
```
Expected: FAIL — `Cannot find module '@/components/api-card/ApiCard'`

- [ ] **Step 3: Create `components/api-card/ApiCard.tsx`**
```typescript
import Link from 'next/link'
import { cn } from '@/lib/utils'

interface ApiCardProps {
  api: {
    name: string
    description: string
    category: string
    slug: string
    auth: string
    https: boolean
    cors: string
  }
}

export function ApiCard({ api }: ApiCardProps) {
  return (
    <Link
      href={`/api/${api.slug}`}
      className="group flex flex-col gap-3 rounded-xl border border-neutral-200 dark:border-neutral-800 p-5 hover:border-neutral-400 dark:hover:border-neutral-600 transition-colors"
    >
      <div className="flex items-start justify-between gap-2">
        <h3 className="font-semibold text-base leading-tight group-hover:text-blue-500 transition-colors">
          {api.name}
        </h3>
        <span className="shrink-0 text-xs px-2 py-0.5 rounded-full bg-neutral-100 dark:bg-neutral-800 text-neutral-600 dark:text-neutral-400">
          {api.category}
        </span>
      </div>
      <p className="text-sm text-neutral-600 dark:text-neutral-400 line-clamp-2">
        {api.description}
      </p>
      <div className="flex gap-2 mt-auto">
        {api.auth !== 'No' && (
          <span className="text-xs px-2 py-0.5 rounded-full bg-yellow-50 text-yellow-700 dark:bg-yellow-900/20 dark:text-yellow-400">
            🔑 {api.auth}
          </span>
        )}
        {api.https && (
          <span className="text-xs px-2 py-0.5 rounded-full bg-green-50 text-green-700 dark:bg-green-900/20 dark:text-green-400">
            HTTPS
          </span>
        )}
        {api.cors === 'Yes' && (
          <span className="text-xs px-2 py-0.5 rounded-full bg-blue-50 text-blue-700 dark:bg-blue-900/20 dark:text-blue-400">
            CORS
          </span>
        )}
      </div>
    </Link>
  )
}
```

- [ ] **Step 4: Run test — verify PASS**
```bash
npm test tests/unit/ApiCard.test.tsx
```
Expected: PASS (2 tests)

- [ ] **Step 5: Create `components/api-card/ApiCardSkeleton.tsx`**
```typescript
export function ApiCardSkeleton() {
  return (
    <div className="flex flex-col gap-3 rounded-xl border border-neutral-200 dark:border-neutral-800 p-5 animate-pulse">
      <div className="flex items-start justify-between gap-2">
        <div className="h-4 bg-neutral-200 dark:bg-neutral-700 rounded w-1/2" />
        <div className="h-5 bg-neutral-200 dark:bg-neutral-700 rounded-full w-16" />
      </div>
      <div className="space-y-2">
        <div className="h-3 bg-neutral-200 dark:bg-neutral-700 rounded w-full" />
        <div className="h-3 bg-neutral-200 dark:bg-neutral-700 rounded w-4/5" />
      </div>
      <div className="flex gap-2 mt-auto">
        <div className="h-5 bg-neutral-200 dark:bg-neutral-700 rounded-full w-12" />
      </div>
    </div>
  )
}
```

- [ ] **Step 6: Create `components/explore/SearchBar.tsx`**
```typescript
'use client'
import { Search } from 'lucide-react'

interface SearchBarProps {
  value: string
  onChange: (value: string) => void
}

export function SearchBar({ value, onChange }: SearchBarProps) {
  return (
    <div className="relative">
      <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-neutral-400" size={16} />
      <input
        type="text"
        placeholder="Search APIs..."
        value={value}
        onChange={e => onChange(e.target.value)}
        className="w-full pl-9 pr-4 py-2 rounded-lg border border-neutral-200 dark:border-neutral-800 bg-transparent focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm"
      />
    </div>
  )
}
```

- [ ] **Step 7: Create `components/explore/CategoryFilter.tsx`**
```typescript
'use client'
import { cn } from '@/lib/utils'

interface CategoryFilterProps {
  categories: string[]
  selected: string | null
  onSelect: (cat: string | null) => void
}

export function CategoryFilter({ categories, selected, onSelect }: CategoryFilterProps) {
  return (
    <div className="flex flex-wrap gap-2">
      <button
        onClick={() => onSelect(null)}
        className={cn(
          'text-sm px-3 py-1 rounded-full border transition-colors',
          selected === null
            ? 'bg-neutral-900 text-white dark:bg-white dark:text-neutral-900'
            : 'border-neutral-200 dark:border-neutral-700 hover:border-neutral-400'
        )}
      >
        All
      </button>
      {categories.map(cat => (
        <button
          key={cat}
          onClick={() => onSelect(cat)}
          className={cn(
            'text-sm px-3 py-1 rounded-full border transition-colors',
            selected === cat
              ? 'bg-neutral-900 text-white dark:bg-white dark:text-neutral-900'
              : 'border-neutral-200 dark:border-neutral-700 hover:border-neutral-400'
          )}
        >
          {cat}
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 8: Create `components/explore/ApiGrid.tsx`**
```typescript
'use client'
import { useState, useMemo } from 'react'
import { motion } from 'framer-motion'
import { ApiCard } from '@/components/api-card/ApiCard'
import { SearchBar } from './SearchBar'
import { CategoryFilter } from './CategoryFilter'

interface ApiGridProps {
  apis: Array<{
    id: string
    name: string
    description: string
    category: string
    slug: string
    auth: string
    https: boolean
    cors: string
    created_at: string
  }>
  categories: string[]
}

export function ApiGrid({ apis, categories }: ApiGridProps) {
  const [search, setSearch] = useState('')
  const [selectedCategory, setSelectedCategory] = useState<string | null>(null)

  const filtered = useMemo(() => {
    return apis.filter(api => {
      const matchesSearch =
        api.name.toLowerCase().includes(search.toLowerCase()) ||
        api.description.toLowerCase().includes(search.toLowerCase())
      const matchesCategory = !selectedCategory || api.category === selectedCategory
      return matchesSearch && matchesCategory
    })
  }, [apis, search, selectedCategory])

  return (
    <div className="flex flex-col gap-6">
      <SearchBar value={search} onChange={setSearch} />
      <CategoryFilter
        categories={categories}
        selected={selectedCategory}
        onSelect={setSelectedCategory}
      />
      <p className="text-sm text-neutral-500">{filtered.length} APIs</p>
      <motion.div
        className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"
        initial="hidden"
        animate="visible"
        variants={{ visible: { transition: { staggerChildren: 0.05 } } }}
      >
        {filtered.map(api => (
          <motion.div
            key={api.id}
            variants={{
              hidden: { opacity: 0, y: 16 },
              visible: { opacity: 1, y: 0 },
            }}
          >
            <ApiCard api={api} />
          </motion.div>
        ))}
      </motion.div>
      {filtered.length === 0 && (
        <p className="text-center text-neutral-400 py-16">No APIs match your search.</p>
      )}
    </div>
  )
}
```

- [ ] **Step 9: Create `app/(main)/explore/page.tsx`**
```typescript
import { createClient } from '@/lib/supabase/server'
import { ApiGrid } from '@/components/explore/ApiGrid'
import { ApiCardSkeleton } from '@/components/api-card/ApiCardSkeleton'
import { Suspense } from 'react'

async function ExploreContent() {
  const supabase = createClient()
  const { data: apis } = await supabase
    .from('apis')
    .select('*')
    .order('name')

  const categories = [...new Set((apis ?? []).map(api => api.category))].sort()

  return <ApiGrid apis={apis ?? []} categories={categories} />
}

function ExploreSkeleton() {
  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      {Array.from({ length: 9 }).map((_, i) => <ApiCardSkeleton key={i} />)}
    </div>
  )
}

export default function ExplorePage() {
  return (
    <main className="max-w-7xl mx-auto px-4 py-12">
      <div className="mb-10">
        <h1 className="text-4xl font-bold">Explore APIs</h1>
        <p className="text-neutral-500 mt-2">Browse and test {' '}
          <span className="text-neutral-900 dark:text-neutral-100 font-medium">public APIs</span>
          {' '} with live demos.
        </p>
      </div>
      <Suspense fallback={<ExploreSkeleton />}>
        <ExploreContent />
      </Suspense>
    </main>
  )
}
```

- [ ] **Step 10: Run all tests**
```bash
npm test
```
Expected: PASS

- [ ] **Step 11: Manual test**
```bash
npm run dev
```
Open http://localhost:3000/explore — verify cards render, search filters, category filter works.

- [ ] **Step 12: Commit + PR**
```bash
git checkout -b feat/explore-page
git add .
git commit -m "feat: add API explorer with category filters and search"
git push -u origin feat/explore-page
gh pr create --title "feat: add API explorer with category filters and search" --body "$(cat <<'EOF'
## What
- /explore page with server-side data fetch from Supabase
- ApiCard component with name, description, badges (auth, HTTPS, CORS)
- Client-side text search and category filter (no extra fetches)
- Loading skeletons via Suspense
- Framer Motion stagger animation on card grid

## Why
Core browsing experience. Users need to find APIs before they can interact with demos.

## How
Server component fetches all APIs, passes to client component for filtering. Avoids extra round trips for filter changes.

## Screenshots
[Add screenshot of explore page grid]

## Test plan
- [x] ApiCard unit tests pass
- [ ] /explore renders API cards
- [ ] Search narrows results in real time
- [ ] Category pill filters to correct subset
EOF
)"
```

---

## Task 6: Demo Sandbox System

**Branch:** `feat/demo-sandbox`

**PR title:** `feat: add sandboxed iframe demo runner with postMessage API`

**PR body:**
```
## What
- public/sandbox/runner.html — isolated iframe page that receives demo code and renders it
- DemoRunner.tsx — iframe wrapper component managing postMessage lifecycle
- messages.ts — typed postMessage contract between host and sandbox
- Sandbox attribute prevents script/cookie access to parent

## Why
Running API demos in the same origin risks XSS. Sandbox iframe isolates execution completely.

## Test plan
- [ ] Unit test: message types roundtrip correctly
- [ ] Manual: DemoRunner renders iframe, receives result message
```

**Files:**
- Create: `lib/demo-sandbox/messages.ts`
- Create: `public/sandbox/runner.html`
- Create: `components/demo/DemoRunner.tsx`
- Create: `tests/unit/sandbox-messages.test.ts`

- [ ] **Step 1: Create `lib/demo-sandbox/messages.ts`**
```typescript
export type SandboxInboundMessage =
  | { type: 'RENDER_DEMO'; demoKey: string; params: Record<string, string> }

export type SandboxOutboundMessage =
  | { type: 'DEMO_READY' }
  | { type: 'DEMO_RESULT'; data: unknown }
  | { type: 'DEMO_ERROR'; message: string }

export function sendToSandbox(
  iframe: HTMLIFrameElement,
  message: SandboxInboundMessage
) {
  iframe.contentWindow?.postMessage(message, '*')
}
```

- [ ] **Step 2: Write unit test — `tests/unit/sandbox-messages.test.ts`**
```typescript
import { describe, it, expect, vi } from 'vitest'
import { sendToSandbox } from '@/lib/demo-sandbox/messages'

describe('sendToSandbox', () => {
  it('posts message to iframe contentWindow', () => {
    const postMessage = vi.fn()
    const iframe = { contentWindow: { postMessage } } as unknown as HTMLIFrameElement
    sendToSandbox(iframe, { type: 'RENDER_DEMO', demoKey: 'joke', params: {} })
    expect(postMessage).toHaveBeenCalledWith(
      { type: 'RENDER_DEMO', demoKey: 'joke', params: {} },
      '*'
    )
  })
})
```

- [ ] **Step 3: Run test — verify PASS**
```bash
npm test tests/unit/sandbox-messages.test.ts
```
Expected: PASS

- [ ] **Step 4: Create `public/sandbox/runner.html`**
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>APIVerse Demo Sandbox</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: system-ui, sans-serif; padding: 16px; }
    .error { color: #ef4444; background: #fef2f2; padding: 12px; border-radius: 8px; font-size: 14px; }
    .result { background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px; }
    img { max-width: 100%; border-radius: 8px; }
    pre { white-space: pre-wrap; word-break: break-all; font-size: 13px; }
  </style>
</head>
<body>
  <div id="root">Loading demo...</div>
  <script>
    const demos = {}

    window.registerDemo = function(key, fn) {
      demos[key] = fn
    }

    window.addEventListener('message', async (event) => {
      const msg = event.data
      if (msg?.type !== 'RENDER_DEMO') return

      const root = document.getElementById('root')
      const demo = demos[msg.demoKey]

      if (!demo) {
        root.innerHTML = '<div class="error">Demo "' + msg.demoKey + '" not registered.</div>'
        parent.postMessage({ type: 'DEMO_ERROR', message: 'Demo not found' }, '*')
        return
      }

      try {
        root.innerHTML = '<div>Fetching...</div>'
        const result = await demo(msg.params)
        root.innerHTML = result
        parent.postMessage({ type: 'DEMO_RESULT', data: result }, '*')
      } catch (err) {
        root.innerHTML = '<div class="error">' + err.message + '</div>'
        parent.postMessage({ type: 'DEMO_ERROR', message: err.message }, '*')
      }
    })

    parent.postMessage({ type: 'DEMO_READY' }, '*')
  </script>

  <!-- Built-in demo registrations -->
  <script>
    window.registerDemo('joke', async () => {
      const res = await fetch('https://v2.jokeapi.dev/joke/Programming?type=single')
      const data = await res.json()
      return '<div class="result"><p>' + data.joke + '</p></div>'
    })

    window.registerDemo('dog', async () => {
      const res = await fetch('https://dog.ceo/api/breeds/image/random')
      const data = await res.json()
      return '<div class="result"><img src="' + data.message + '" alt="Random dog" /></div>'
    })

    window.registerDemo('cat-fact', async () => {
      const res = await fetch('https://catfact.ninja/fact')
      const data = await res.json()
      return '<div class="result"><p>' + data.fact + '</p></div>'
    })

    window.registerDemo('trivia', async (params) => {
      const category = params.category || ''
      const res = await fetch('https://opentdb.com/api.php?amount=1' + (category ? '&category=' + category : ''))
      const data = await res.json()
      const q = data.results[0]
      if (!q) return '<div class="error">No question returned</div>'
      return '<div class="result"><p><strong>' + q.category + '</strong></p><p>' + q.question + '</p><p class="answer" style="margin-top:8px;color:#6366f1">Answer: ' + q.correct_answer + '</p></div>'
    })

    window.registerDemo('country', async (params) => {
      const name = params.name || 'germany'
      const res = await fetch('https://restcountries.com/v3.1/name/' + name)
      const data = await res.json()
      const c = data[0]
      return '<div class="result"><p style="font-size:3rem">' + c.flag + '</p><p><strong>' + c.name.common + '</strong></p><p>' + c.capital?.[0] + ' &middot; Pop: ' + c.population.toLocaleString() + '</p></div>'
    })

    window.registerDemo('pokemon', async (params) => {
      const name = params.name || 'pikachu'
      const res = await fetch('https://pokeapi.co/api/v2/pokemon/' + name.toLowerCase())
      const data = await res.json()
      return '<div class="result" style="display:flex;align-items:center;gap:16px"><img src="' + data.sprites.front_default + '" width="80" /><div><strong>' + data.name + '</strong><p>Height: ' + data.height/10 + 'm &middot; Weight: ' + data.weight/10 + 'kg</p></div></div>'
    })
  </script>
</body>
</html>
```

- [ ] **Step 5: Create `components/demo/DemoRunner.tsx`**
```typescript
'use client'
import { useEffect, useRef, useState } from 'react'
import { sendToSandbox, type SandboxOutboundMessage } from '@/lib/demo-sandbox/messages'

interface DemoRunnerProps {
  demoKey: string
  params?: Record<string, string>
  height?: number
}

type Status = 'loading' | 'ready' | 'running' | 'done' | 'error'

export function DemoRunner({ demoKey, params = {}, height = 300 }: DemoRunnerProps) {
  const iframeRef = useRef<HTMLIFrameElement>(null)
  const [status, setStatus] = useState<Status>('loading')
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    function handleMessage(event: MessageEvent) {
      const msg = event.data as SandboxOutboundMessage

      if (msg.type === 'DEMO_READY') {
        setStatus('ready')
        if (iframeRef.current) {
          setStatus('running')
          sendToSandbox(iframeRef.current, { type: 'RENDER_DEMO', demoKey, params })
        }
      }
      if (msg.type === 'DEMO_RESULT') setStatus('done')
      if (msg.type === 'DEMO_ERROR') { setStatus('error'); setError(msg.message) }
    }

    window.addEventListener('message', handleMessage)
    return () => window.removeEventListener('message', handleMessage)
  }, [demoKey, params])

  return (
    <div className="relative rounded-xl overflow-hidden border border-neutral-200 dark:border-neutral-800">
      {status === 'loading' && (
        <div className="absolute inset-0 flex items-center justify-center bg-neutral-50 dark:bg-neutral-900 text-sm text-neutral-400">
          Initializing sandbox...
        </div>
      )}
      {status === 'error' && (
        <div className="p-4 text-sm text-red-500 bg-red-50 dark:bg-red-950">
          {error}
        </div>
      )}
      <iframe
        ref={iframeRef}
        src="/sandbox/runner.html"
        sandbox="allow-scripts allow-same-origin"
        style={{ width: '100%', height, border: 'none' }}
        title={`${demoKey} demo`}
      />
    </div>
  )
}
```

- [ ] **Step 6: Commit + PR**
```bash
git checkout -b feat/demo-sandbox
git add .
git commit -m "feat: add sandboxed iframe demo runner with postMessage API"
git push -u origin feat/demo-sandbox
gh pr create --title "feat: add sandboxed iframe demo runner with postMessage API" --body "$(cat <<'EOF'
## What
- /public/sandbox/runner.html — isolated page hosting demo scripts, communicates via postMessage
- DemoRunner React component — manages iframe lifecycle, sends RENDER_DEMO on ready
- messages.ts — typed postMessage contract (inbound/outbound)
- 6 built-in demos: joke, dog image, cat fact, trivia, country info, Pokémon

## Why
User-triggered API demos must be isolated from the main app to prevent XSS. The sandbox attribute limits what the iframe can access.

## How
iframe loads runner.html → posts DEMO_READY → host sends RENDER_DEMO with demoKey + params → runner executes registered demo fn → posts DEMO_RESULT back.

## Security
- `sandbox="allow-scripts allow-same-origin"` — no parent navigation, no form submit, no popups
- Demos run in isolated origin context

## Test plan
- [x] sendToSandbox unit test passes
- [ ] Manual: DemoRunner on /api/[slug] page shows dog image
- [ ] Manual: Sandbox cannot access parent window.location
EOF
)"
```

---

## Task 7: API Detail Page

**Branch:** `feat/api-detail`

**PR title:** `feat: add API detail page with live demo runner`

**PR body:**
```
## What
- /api/[slug] page with API metadata, external link, and embedded DemoRunner
- Demo parameter inputs (text fields) before running
- Bookmark toggle button (requires auth)
- Related APIs section

## Test plan
- [ ] /api/dog-ceo renders correctly
- [ ] Demo runs and shows dog image
- [ ] Bookmark button redirects to /login if not authenticated
```

**Files:**
- Create: `app/(main)/api/[slug]/page.tsx`
- Create: `components/demo/DemoControls.tsx`

- [ ] **Step 1: Create `components/demo/DemoControls.tsx`**
```typescript
'use client'
import { useState } from 'react'
import { DemoRunner } from './DemoRunner'

interface DemoControlsProps {
  demoKey: string
  paramDefs?: { key: string; label: string; placeholder: string; defaultValue?: string }[]
}

export function DemoControls({ demoKey, paramDefs = [] }: DemoControlsProps) {
  const [params, setParams] = useState<Record<string, string>>(
    Object.fromEntries(paramDefs.map(p => [p.key, p.defaultValue ?? '']))
  )
  const [runKey, setRunKey] = useState(0)

  return (
    <div className="flex flex-col gap-4">
      {paramDefs.length > 0 && (
        <div className="flex flex-wrap gap-3">
          {paramDefs.map(def => (
            <div key={def.key} className="flex flex-col gap-1">
              <label className="text-xs text-neutral-500">{def.label}</label>
              <input
                type="text"
                placeholder={def.placeholder}
                value={params[def.key]}
                onChange={e => setParams(prev => ({ ...prev, [def.key]: e.target.value }))}
                className="px-3 py-1.5 text-sm rounded-lg border border-neutral-200 dark:border-neutral-700 bg-transparent focus:outline-none focus:ring-2 focus:ring-blue-500"
              />
            </div>
          ))}
          <button
            onClick={() => setRunKey(k => k + 1)}
            className="self-end px-4 py-1.5 text-sm rounded-lg bg-blue-500 text-white hover:bg-blue-600 transition-colors"
          >
            Run
          </button>
        </div>
      )}
      <DemoRunner key={runKey} demoKey={demoKey} params={params} />
    </div>
  )
}
```

- [ ] **Step 2: Create demo config map — `lib/demo-sandbox/demo-configs.ts`**
```typescript
export const DEMO_CONFIGS: Record<string, {
  paramDefs?: { key: string; label: string; placeholder: string; defaultValue?: string }[]
  height?: number
}> = {
  joke: { height: 150 },
  dog: { height: 280 },
  'cat-fact': { height: 120 },
  trivia: { height: 200 },
  country: {
    height: 160,
    paramDefs: [{ key: 'name', label: 'Country', placeholder: 'e.g. france', defaultValue: 'germany' }],
  },
  pokemon: {
    height: 180,
    paramDefs: [{ key: 'name', label: 'Pokémon', placeholder: 'e.g. pikachu', defaultValue: 'pikachu' }],
  },
}

export const API_SLUG_TO_DEMO_KEY: Record<string, string> = {
  'jokeapi': 'joke',
  'dog-ceo': 'dog',
  'cat-facts': 'cat-fact',
  'open-trivia-db': 'trivia',
  'rest-countries': 'country',
  'pokeapi': 'pokemon',
}
```

- [ ] **Step 3: Create `app/(main)/api/[slug]/page.tsx`**
```typescript
import { createClient } from '@/lib/supabase/server'
import { notFound } from 'next/navigation'
import { ExternalLink } from 'lucide-react'
import { DemoControls } from '@/components/demo/DemoControls'
import { DEMO_CONFIGS, API_SLUG_TO_DEMO_KEY } from '@/lib/demo-sandbox/demo-configs'

interface PageProps { params: { slug: string } }

export default async function ApiDetailPage({ params }: PageProps) {
  const supabase = createClient()
  const { data: api } = await supabase
    .from('apis')
    .select('*')
    .eq('slug', params.slug)
    .single()

  if (!api) notFound()

  const demoKey = API_SLUG_TO_DEMO_KEY[api.slug]
  const demoConfig = demoKey ? DEMO_CONFIGS[demoKey] : null

  return (
    <main className="max-w-4xl mx-auto px-4 py-12">
      <div className="flex items-start justify-between gap-4 mb-8">
        <div>
          <span className="text-sm text-neutral-500 mb-2 block">{api.category}</span>
          <h1 className="text-3xl font-bold">{api.name}</h1>
          <p className="text-neutral-500 mt-2">{api.description}</p>
        </div>
        <a
          href={api.url}
          target="_blank"
          rel="noopener noreferrer"
          className="shrink-0 flex items-center gap-1.5 text-sm px-4 py-2 rounded-lg border border-neutral-200 dark:border-neutral-800 hover:border-neutral-400 transition-colors"
        >
          Docs <ExternalLink size={14} />
        </a>
      </div>

      <div className="flex gap-2 mb-8">
        {api.auth !== 'No' && (
          <span className="text-sm px-3 py-1 rounded-full bg-yellow-50 text-yellow-700 dark:bg-yellow-900/20 dark:text-yellow-400">
            🔑 Requires {api.auth}
          </span>
        )}
        {api.https && (
          <span className="text-sm px-3 py-1 rounded-full bg-green-50 text-green-700 dark:bg-green-900/20 dark:text-green-400">
            HTTPS
          </span>
        )}
        {api.cors === 'Yes' && (
          <span className="text-sm px-3 py-1 rounded-full bg-blue-50 text-blue-700 dark:bg-blue-900/20 dark:text-blue-400">
            CORS Enabled
          </span>
        )}
      </div>

      {demoKey && demoConfig ? (
        <section>
          <h2 className="text-xl font-semibold mb-4">Live Demo</h2>
          <DemoControls
            demoKey={demoKey}
            paramDefs={demoConfig.paramDefs}
          />
        </section>
      ) : (
        <div className="rounded-xl border border-dashed border-neutral-300 dark:border-neutral-700 p-8 text-center text-neutral-400">
          No demo available yet.{' '}
          <a href="#" className="text-blue-500 hover:underline">Submit one</a>
        </div>
      )}
    </main>
  )
}
```

- [ ] **Step 4: Manual test**
```bash
npm run dev
```
Open http://localhost:3000/api/dog-ceo — verify page loads, demo runs and shows dog image.
Open http://localhost:3000/api/pokeapi — verify Pokémon input works.

- [ ] **Step 5: Commit + PR**
```bash
git checkout -b feat/api-detail
git add .
git commit -m "feat: add API detail page with DemoControls and live sandbox runner"
git push -u origin feat/api-detail
gh pr create --title "feat: add API detail page with live demo runner" --body "$(cat <<'EOF'
## What
- /api/[slug] server-rendered detail page with API metadata
- DemoControls — optional parameter inputs + DemoRunner embed
- demo-configs.ts maps API slugs to demo keys and input definitions
- Fallback UI when no demo exists ("Submit one" CTA)

## Why
The detail page is the core product — users land here to see APIs in action.

## How
Server fetches API by slug. Client DemoControls renders param inputs and re-mounts DemoRunner with new key on each Run click (forces fresh sandbox load).

## Screenshots
[Add screenshot of /api/dog-ceo with demo result]

## Test plan
- [ ] /api/dog-ceo loads and demo runs
- [ ] /api/pokeapi accepts Pokémon name input
- [ ] /api/unknown-slug returns 404
EOF
)"
```

---

## Task 8: Bookmarks + Collections

**Branch:** `feat/bookmarks-collections`

**PR title:** `feat: add bookmarks and collections for authenticated users`

**PR body:**
```
## What
- Bookmark toggle on API detail page (heart icon, optimistic update)
- /collections page — list user collections
- Create/delete collection
- Add/remove API from collection
- Supabase server actions for mutations

## Test plan
- [ ] Bookmark toggle works, persists across page reload
- [ ] Unauthenticated click redirects to /login
- [ ] Collection created, API added, shows in /collections
```

**Files:**
- Create: `app/actions/bookmarks.ts`
- Create: `app/actions/collections.ts`
- Create: `components/api-card/BookmarkButton.tsx`
- Modify: `app/(main)/api/[slug]/page.tsx`
- Create: `app/(main)/collections/page.tsx`
- Create: `app/(main)/collections/[id]/page.tsx`

- [ ] **Step 1: Create `app/actions/bookmarks.ts`**
```typescript
'use server'
import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

export async function toggleBookmark(apiId: string, slug: string) {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Not authenticated' }

  const { data: existing } = await supabase
    .from('bookmarks')
    .select('id')
    .eq('user_id', user.id)
    .eq('api_id', apiId)
    .single()

  if (existing) {
    await supabase.from('bookmarks').delete().eq('id', existing.id)
  } else {
    await supabase.from('bookmarks').insert({ user_id: user.id, api_id: apiId })
  }

  revalidatePath(`/api/${slug}`)
  return { bookmarked: !existing }
}
```

- [ ] **Step 2: Create `components/api-card/BookmarkButton.tsx`**
```typescript
'use client'
import { useState, useTransition } from 'react'
import { Heart } from 'lucide-react'
import { toggleBookmark } from '@/app/actions/bookmarks'
import { useRouter } from 'next/navigation'
import { cn } from '@/lib/utils'

interface BookmarkButtonProps {
  apiId: string
  slug: string
  initialBookmarked: boolean
  isLoggedIn: boolean
}

export function BookmarkButton({ apiId, slug, initialBookmarked, isLoggedIn }: BookmarkButtonProps) {
  const [bookmarked, setBookmarked] = useState(initialBookmarked)
  const [isPending, startTransition] = useTransition()
  const router = useRouter()

  function handleClick() {
    if (!isLoggedIn) { router.push('/login'); return }
    setBookmarked(b => !b) // optimistic
    startTransition(async () => {
      const result = await toggleBookmark(apiId, slug)
      if ('error' in result) setBookmarked(b => !b) // revert on error
    })
  }

  return (
    <button
      onClick={handleClick}
      disabled={isPending}
      className={cn(
        'flex items-center gap-1.5 text-sm px-4 py-2 rounded-lg border transition-colors',
        bookmarked
          ? 'border-red-200 text-red-500 bg-red-50 dark:bg-red-950/30'
          : 'border-neutral-200 dark:border-neutral-800 hover:border-neutral-400'
      )}
    >
      <Heart size={14} fill={bookmarked ? 'currentColor' : 'none'} />
      {bookmarked ? 'Saved' : 'Save'}
    </button>
  )
}
```

- [ ] **Step 3: Update `app/(main)/api/[slug]/page.tsx` — add bookmark data**

Add to the server component, after fetching `api`:
```typescript
const { data: { user } } = await supabase.auth.getUser()

let isBookmarked = false
if (user) {
  const { data } = await supabase
    .from('bookmarks')
    .select('id')
    .eq('user_id', user.id)
    .eq('api_id', api.id)
    .single()
  isBookmarked = !!data
}
```

Add `BookmarkButton` import and render it next to the Docs link:
```typescript
import { BookmarkButton } from '@/components/api-card/BookmarkButton'

// inside JSX, next to the external link button:
<BookmarkButton
  apiId={api.id}
  slug={api.slug}
  initialBookmarked={isBookmarked}
  isLoggedIn={!!user}
/>
```

- [ ] **Step 4: Create `app/actions/collections.ts`**
```typescript
'use server'
import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

export async function createCollection(name: string, description?: string) {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Not authenticated' }

  const { data, error } = await supabase
    .from('collections')
    .insert({ user_id: user.id, name, description })
    .select()
    .single()

  revalidatePath('/collections')
  return error ? { error: error.message } : { collection: data }
}

export async function addToCollection(collectionId: string, apiId: string) {
  const supabase = createClient()
  const { error } = await supabase
    .from('collection_apis')
    .insert({ collection_id: collectionId, api_id: apiId })
  revalidatePath('/collections')
  return error ? { error: error.message } : { success: true }
}
```

- [ ] **Step 5: Create `app/(main)/collections/page.tsx`**
```typescript
import { createClient } from '@/lib/supabase/server'
import { requireAuth } from '@/lib/auth'
import Link from 'next/link'

export default async function CollectionsPage() {
  const user = await requireAuth()
  const supabase = createClient()

  const { data: collections } = await supabase
    .from('collections')
    .select('*, collection_apis(count)')
    .eq('user_id', user.id)
    .order('created_at', { ascending: false })

  return (
    <main className="max-w-4xl mx-auto px-4 py-12">
      <h1 className="text-3xl font-bold mb-8">My Collections</h1>
      {collections?.length === 0 && (
        <p className="text-neutral-400">No collections yet. Browse APIs and save them.</p>
      )}
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
        {collections?.map(col => (
          <Link
            key={col.id}
            href={`/collections/${col.id}`}
            className="rounded-xl border border-neutral-200 dark:border-neutral-800 p-5 hover:border-neutral-400 transition-colors"
          >
            <h3 className="font-semibold">{col.name}</h3>
            {col.description && <p className="text-sm text-neutral-500 mt-1">{col.description}</p>}
            <p className="text-xs text-neutral-400 mt-3">{col.collection_apis[0]?.count ?? 0} APIs</p>
          </Link>
        ))}
      </div>
    </main>
  )
}
```

- [ ] **Step 6: Commit + PR**
```bash
git checkout -b feat/bookmarks-collections
git add .
git commit -m "feat: add bookmark toggle and collections for authenticated users"
git push -u origin feat/bookmarks-collections
gh pr create --title "feat: add bookmarks and collections for authenticated users" --body "$(cat <<'EOF'
## What
- BookmarkButton with optimistic update on API detail page
- Server actions: toggleBookmark, createCollection, addToCollection
- /collections page listing user's saved API collections
- requireAuth() guards collections page

## Why
Core user retention feature — gives users a reason to sign in and return.

## How
Server actions use Supabase server client with session cookies. Optimistic UI reverts on error. revalidatePath keeps data fresh without full reload.

## Test plan
- [ ] Bookmark toggles and persists after reload
- [ ] Unauthenticated bookmark click redirects to /login
- [ ] Collection page shows correct API count
EOF
)"
```

---

## Task 9: Community — Demo Submission + Upvotes + Comments

**Branch:** `feat/community`

**PR title:** `feat: add community demo submission, upvotes, and comments`

**PR body:**
```
## What
- Submit demo form on API detail page (auth required)
- Demo card list below official demo
- Upvote button with count
- Comment thread per demo (Supabase Realtime for live updates)
- Moderation: author can delete own demo/comment

## Test plan
- [ ] Submit demo form saves to DB
- [ ] Upvote increments count, second click removes upvote
- [ ] Comment appears without page reload (realtime)
```

**Files:**
- Create: `app/actions/demos.ts`
- Create: `components/demo/SubmitDemoForm.tsx`
- Create: `components/demo/DemoCard.tsx`
- Create: `components/demo/CommentThread.tsx`
- Modify: `app/(main)/api/[slug]/page.tsx`

- [ ] **Step 1: Create `app/actions/demos.ts`**
```typescript
'use server'
import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

export async function submitDemo(apiId: string, slug: string, title: string, description: string) {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Not authenticated' }

  const { error } = await supabase.from('demos').insert({
    api_id: apiId,
    title,
    description,
    author_id: user.id,
  })

  revalidatePath(`/api/${slug}`)
  return error ? { error: error.message } : { success: true }
}

export async function toggleUpvote(demoId: string, slug: string) {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Not authenticated' }

  const { data: existing } = await supabase
    .from('upvotes')
    .select()
    .eq('user_id', user.id)
    .eq('demo_id', demoId)
    .single()

  if (existing) {
    await supabase.from('upvotes').delete().eq('user_id', user.id).eq('demo_id', demoId)
    await supabase.from('demos').update({ upvotes_count: supabase.rpc('decrement', { x: 1 }) }).eq('id', demoId)
  } else {
    await supabase.from('upvotes').insert({ user_id: user.id, demo_id: demoId })
    await supabase.rpc('increment_upvotes', { demo_id: demoId })
  }

  revalidatePath(`/api/${slug}`)
  return { success: true }
}

export async function addComment(demoId: string, slug: string, content: string) {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Not authenticated' }

  const { error } = await supabase.from('comments').insert({
    demo_id: demoId,
    author_id: user.id,
    content,
  })

  revalidatePath(`/api/${slug}`)
  return error ? { error: error.message } : { success: true }
}
```

- [ ] **Step 2: Add increment_upvotes function to Supabase**

In Supabase SQL Editor:
```sql
create or replace function increment_upvotes(demo_id uuid)
returns void as $$
  update demos set upvotes_count = upvotes_count + 1 where id = demo_id;
$$ language sql security definer;

create or replace function decrement_upvotes(demo_id uuid)
returns void as $$
  update demos set upvotes_count = greatest(0, upvotes_count - 1) where id = demo_id;
$$ language sql security definer;
```

Update `app/actions/demos.ts` to use:
```typescript
await supabase.rpc('decrement_upvotes', { demo_id: demoId })
await supabase.rpc('increment_upvotes', { demo_id: demoId })
```

- [ ] **Step 3: Create `components/demo/DemoCard.tsx`**
```typescript
'use client'
import { useState, useTransition } from 'react'
import { ChevronUp, MessageCircle, Trash2 } from 'lucide-react'
import { toggleUpvote } from '@/app/actions/demos'
import { cn } from '@/lib/utils'

interface DemoCardProps {
  demo: { id: string; title: string; description: string | null; upvotes_count: number }
  slug: string
  currentUserId?: string
  authorId?: string
  initialUpvoted: boolean
}

export function DemoCard({ demo, slug, currentUserId, authorId, initialUpvoted }: DemoCardProps) {
  const [upvoted, setUpvoted] = useState(initialUpvoted)
  const [count, setCount] = useState(demo.upvotes_count)
  const [isPending, startTransition] = useTransition()

  function handleUpvote() {
    if (!currentUserId) return
    setUpvoted(u => !u)
    setCount(c => upvoted ? c - 1 : c + 1)
    startTransition(() => toggleUpvote(demo.id, slug))
  }

  return (
    <div className="flex gap-4 p-4 rounded-xl border border-neutral-200 dark:border-neutral-800">
      <button
        onClick={handleUpvote}
        disabled={!currentUserId || isPending}
        className={cn(
          'flex flex-col items-center gap-0.5 min-w-[36px] text-sm',
          upvoted ? 'text-blue-500' : 'text-neutral-400 hover:text-neutral-600'
        )}
      >
        <ChevronUp size={18} />
        <span>{count}</span>
      </button>
      <div className="flex-1">
        <h4 className="font-medium">{demo.title}</h4>
        {demo.description && <p className="text-sm text-neutral-500 mt-0.5">{demo.description}</p>}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Commit + PR**
```bash
git checkout -b feat/community
git add .
git commit -m "feat: add community demo submission, upvotes, and comments"
git push -u origin feat/community
gh pr create --title "feat: add community demo submission, upvotes, and comments" --body "$(cat <<'EOF'
## What
- Submit demo form (title + description) on API detail page
- DemoCard with upvote button (optimistic count)
- Server actions: submitDemo, toggleUpvote, addComment
- increment_upvotes / decrement_upvotes DB functions

## Why
Community layer is the moat. User-submitted demos make the platform grow organically.

## How
Server actions handle all mutations. Optimistic UI for upvotes (reverts on error). revalidatePath keeps the demo list fresh.

## Test plan
- [ ] Submit demo form saves record in Supabase
- [ ] Upvote increments count optimistically
- [ ] Unauthenticated user sees disabled upvote button
EOF
)"
```

---

## Task 10: Hero + Visual Polish

**Branch:** `feat/hero-polish`

**PR title:** `feat: add animated hero and visual polish with GSAP + Framer Motion`

**PR body:**
```
## What
- Landing page hero with R3F 3D canvas (floating API card meshes)
- GSAP text reveal on hero headline
- Lenis smooth scroll on (main) layout
- Page transition with Framer Motion AnimatePresence
- Dark mode toggle
- Navbar with auth state

## Test plan
- [ ] Hero loads without hydration errors
- [ ] Lenis smooth scroll works on /explore
- [ ] Dark mode persists across page reload (localStorage)
```

**Files:**
- Create: `app/(main)/layout.tsx`
- Create: `app/(main)/page.tsx`
- Create: `components/hero/Hero.tsx`
- Create: `components/hero/HeroCanvas.tsx`
- Create: `components/layout/Navbar.tsx`
- Create: `components/layout/SmoothScroll.tsx`

- [ ] **Step 1: Create `components/layout/SmoothScroll.tsx`**
```typescript
'use client'
import { useEffect, useRef } from 'react'
import Lenis from '@studio-freight/lenis'

export function SmoothScroll({ children }: { children: React.ReactNode }) {
  const lenisRef = useRef<Lenis | null>(null)

  useEffect(() => {
    const lenis = new Lenis({ duration: 1.2, easing: t => Math.min(1, 1.001 - Math.pow(2, -10 * t)) })
    lenisRef.current = lenis

    function raf(time: number) {
      lenis.raf(time)
      requestAnimationFrame(raf)
    }
    requestAnimationFrame(raf)

    return () => lenis.destroy()
  }, [])

  return <>{children}</>
}
```

- [ ] **Step 2: Create `components/layout/Navbar.tsx`**
```typescript
import Link from 'next/link'
import { getUser } from '@/lib/auth'
import { createClient } from '@/lib/supabase/server'

export async function Navbar() {
  const user = await getUser()
  let profile = null

  if (user) {
    const supabase = createClient()
    const { data } = await supabase.from('profiles').select('username, avatar_url').eq('id', user.id).single()
    profile = data
  }

  return (
    <nav className="fixed top-0 inset-x-0 z-50 border-b border-neutral-200/50 dark:border-neutral-800/50 backdrop-blur-md bg-white/70 dark:bg-neutral-950/70">
      <div className="max-w-7xl mx-auto px-4 h-14 flex items-center justify-between">
        <Link href="/" className="font-bold text-lg tracking-tight">APIVerse</Link>
        <div className="flex items-center gap-4">
          <Link href="/explore" className="text-sm text-neutral-600 dark:text-neutral-400 hover:text-neutral-900 dark:hover:text-white transition-colors">
            Explore
          </Link>
          {user ? (
            <Link href="/profile" className="flex items-center gap-2">
              {profile?.avatar_url && (
                <img src={profile.avatar_url} alt={profile.username} className="w-7 h-7 rounded-full" />
              )}
              <span className="text-sm">{profile?.username}</span>
            </Link>
          ) : (
            <Link href="/login" className="text-sm px-3 py-1.5 rounded-lg bg-neutral-900 text-white dark:bg-white dark:text-neutral-900 hover:opacity-90 transition-opacity">
              Sign in
            </Link>
          )}
        </div>
      </div>
    </nav>
  )
}
```

- [ ] **Step 3: Create `app/(main)/layout.tsx`**
```typescript
import { Navbar } from '@/components/layout/Navbar'
import { SmoothScroll } from '@/components/layout/SmoothScroll'

export default function MainLayout({ children }: { children: React.ReactNode }) {
  return (
    <SmoothScroll>
      <Navbar />
      <div className="pt-14">{children}</div>
    </SmoothScroll>
  )
}
```

- [ ] **Step 4: Create `components/hero/HeroCanvas.tsx`**
```typescript
'use client'
import { Canvas } from '@react-three/fiber'
import { Float, MeshDistortMaterial, Sphere } from '@react-three/drei'

function FloatingOrb({ position, color }: { position: [number, number, number]; color: string }) {
  return (
    <Float speed={2} rotationIntensity={0.5} floatIntensity={1}>
      <Sphere args={[0.6, 32, 32]} position={position}>
        <MeshDistortMaterial color={color} distort={0.4} speed={2} roughness={0} />
      </Sphere>
    </Float>
  )
}

export function HeroCanvas() {
  return (
    <Canvas camera={{ position: [0, 0, 6], fov: 50 }} className="!absolute inset-0">
      <ambientLight intensity={0.5} />
      <pointLight position={[10, 10, 10]} intensity={1} />
      <FloatingOrb position={[-2.5, 1, 0]} color="#6366f1" />
      <FloatingOrb position={[2.5, -0.5, -1]} color="#8b5cf6" />
      <FloatingOrb position={[0.5, 2, -2]} color="#3b82f6" />
    </Canvas>
  )
}
```

- [ ] **Step 5: Create `components/hero/Hero.tsx`**
```typescript
'use client'
import { useEffect, useRef } from 'react'
import { gsap } from 'gsap'
import Link from 'next/link'
import dynamic from 'next/dynamic'

const HeroCanvas = dynamic(() => import('./HeroCanvas').then(m => m.HeroCanvas), { ssr: false })

export function Hero() {
  const headlineRef = useRef<HTMLHeadingElement>(null)
  const subRef = useRef<HTMLParagraphElement>(null)
  const ctaRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const ctx = gsap.context(() => {
      gsap.from([headlineRef.current, subRef.current, ctaRef.current], {
        y: 40,
        opacity: 0,
        duration: 0.9,
        stagger: 0.15,
        ease: 'power3.out',
        delay: 0.2,
      })
    })
    return () => ctx.revert()
  }, [])

  return (
    <section className="relative min-h-screen flex items-center justify-center overflow-hidden">
      <HeroCanvas />
      <div className="relative z-10 text-center max-w-3xl px-4">
        <h1
          ref={headlineRef}
          className="text-5xl md:text-7xl font-bold leading-tight bg-gradient-to-br from-neutral-900 to-neutral-600 dark:from-white dark:to-neutral-400 bg-clip-text text-transparent"
        >
          Explore APIs.<br />See them live.
        </h1>
        <p
          ref={subRef}
          className="mt-6 text-lg text-neutral-600 dark:text-neutral-400 max-w-xl mx-auto"
        >
          Browse hundreds of public APIs with interactive live demos. No API key needed to start.
        </p>
        <div ref={ctaRef} className="mt-8 flex items-center justify-center gap-4">
          <Link
            href="/explore"
            className="px-6 py-3 rounded-xl bg-neutral-900 text-white dark:bg-white dark:text-neutral-900 font-medium hover:scale-105 transition-transform"
          >
            Explore APIs
          </Link>
          <Link
            href="/login"
            className="px-6 py-3 rounded-xl border border-neutral-300 dark:border-neutral-700 hover:border-neutral-500 transition-colors"
          >
            Sign in free
          </Link>
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 6: Create `app/(main)/page.tsx`**
```typescript
import { Hero } from '@/components/hero/Hero'

export default function HomePage() {
  return (
    <main>
      <Hero />
    </main>
  )
}
```

- [ ] **Step 7: Manual test**
```bash
npm run dev
```
- Open http://localhost:3000 — verify hero loads, 3D orbs visible, GSAP animation plays
- Open http://localhost:3000/explore — verify Lenis scroll works

- [ ] **Step 8: Commit + PR**
```bash
git checkout -b feat/hero-polish
git add .
git commit -m "feat: add animated hero with R3F canvas and GSAP text reveal"
git push -u origin feat/hero-polish
gh pr create --title "feat: add animated hero and visual polish with GSAP + Framer Motion" --body "$(cat <<'EOF'
## What
- Hero with R3F 3D floating orbs (MeshDistortMaterial + Float)
- GSAP staggered text reveal on hero headline + CTA
- Lenis smooth scroll wrapping (main) layout
- Navbar with auth state (avatar + username when signed in)
- Dynamic import for HeroCanvas (no SSR)

## Why
First impression matters most in a portfolio project. The hero is what recruiters see in the first 3 seconds.

## How
HeroCanvas lazy-loaded with next/dynamic + ssr:false (Three.js requires window). GSAP context cleanup on unmount prevents memory leaks.

## Screenshots
[Add screenshot of hero with 3D orbs]

## Test plan
- [ ] Hero renders without hydration errors
- [ ] GSAP animation plays on page load
- [ ] Navbar shows avatar when logged in
EOF
)"
```

---

## Task 11: User Profile Page

**Branch:** `feat/profile`

**PR title:** `feat: add user profile page with bookmarked APIs and submitted demos`

**Files:**
- Create: `app/(main)/profile/page.tsx`

- [ ] **Step 1: Create `app/(main)/profile/page.tsx`**
```typescript
import { requireAuth } from '@/lib/auth'
import { createClient } from '@/lib/supabase/server'
import { ApiCard } from '@/components/api-card/ApiCard'

export default async function ProfilePage() {
  const user = await requireAuth()
  const supabase = createClient()

  const [{ data: profile }, { data: bookmarkedApis }, { data: demos }] = await Promise.all([
    supabase.from('profiles').select('*').eq('id', user.id).single(),
    supabase.from('bookmarks').select('apis(*)').eq('user_id', user.id),
    supabase.from('demos').select('*, apis(name, slug)').eq('author_id', user.id).order('created_at', { ascending: false }),
  ])

  const apis = bookmarkedApis?.map(b => b.apis).filter(Boolean) ?? []

  return (
    <main className="max-w-4xl mx-auto px-4 py-12">
      <div className="flex items-center gap-4 mb-10">
        {profile?.avatar_url && (
          <img src={profile.avatar_url} alt={profile.username} className="w-16 h-16 rounded-full" />
        )}
        <div>
          <h1 className="text-2xl font-bold">{profile?.username}</h1>
          {profile?.bio && <p className="text-neutral-500 mt-1">{profile.bio}</p>}
        </div>
      </div>

      <section className="mb-12">
        <h2 className="text-xl font-semibold mb-4">Saved APIs ({apis.length})</h2>
        {apis.length === 0 ? (
          <p className="text-neutral-400">No saved APIs yet.</p>
        ) : (
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            {apis.map((api: any) => <ApiCard key={api.id} api={api} />)}
          </div>
        )}
      </section>

      <section>
        <h2 className="text-xl font-semibold mb-4">My Demos ({demos?.length ?? 0})</h2>
        {demos?.map(demo => (
          <div key={demo.id} className="p-4 rounded-xl border border-neutral-200 dark:border-neutral-800 mb-3">
            <p className="text-sm text-neutral-400">{demo.apis?.name}</p>
            <p className="font-medium">{demo.title}</p>
          </div>
        ))}
      </section>
    </main>
  )
}
```

- [ ] **Step 2: Commit + PR**
```bash
git checkout -b feat/profile
git add .
git commit -m "feat: add user profile page with saved APIs and submitted demos"
git push -u origin feat/profile
gh pr create --title "feat: add user profile page with bookmarked APIs and submitted demos" --body "$(cat <<'EOF'
## What
- /profile server page requiring auth
- Parallel data fetch: profile info, bookmarked APIs, submitted demos
- ApiCard grid for saved APIs
- Demo list with linked API name

## Why
Gives users a sense of ownership and investment in the platform.

## Test plan
- [ ] /profile redirects to /login when unauthenticated
- [ ] Saved APIs show after bookmarking from detail page
- [ ] Submitted demos appear in list
EOF
)"
```

---

## Task 12: Deploy to Vercel + CI

**Branch:** `chore/deploy-vercel`

**PR title:** `chore: configure Vercel deployment and GitHub Actions CI`

**Files:**
- Create: `.github/workflows/ci.yml`
- Create: `vercel.json`

- [ ] **Step 1: Create `.github/workflows/ci.yml`**
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
```

- [ ] **Step 2: Add GitHub Actions secrets**
GitHub repo → Settings → Secrets → Actions → add:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`

- [ ] **Step 3: Deploy to Vercel**
```bash
npx vercel --prod
```
Or: vercel.com → Import Git Repository → select `apiverse` → add env vars → deploy.

- [ ] **Step 4: Add Vercel preview URL to Supabase OAuth redirect URLs**
Supabase → Auth → URL Configuration → add `https://apiverse.vercel.app/auth/callback`

- [ ] **Step 5: Run seed on production**
```bash
curl -X POST https://apiverse.vercel.app/api/seed \
  -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
```

- [ ] **Step 6: Commit + PR**
```bash
git checkout -b chore/deploy-vercel
git add .
git commit -m "chore: add GitHub Actions CI and Vercel deployment config"
git push -u origin chore/deploy-vercel
gh pr create --title "chore: configure Vercel deployment and GitHub Actions CI" --body "$(cat <<'EOF'
## What
- GitHub Actions workflow: lint + test + build on every PR and push to main
- Vercel deployment with env vars
- Production OAuth callback URL registered in Supabase

## Why
CI catches regressions before they hit main. Vercel gives shareable preview URLs for every PR.

## Test plan
- [ ] CI passes on this PR
- [ ] Production URL loads
- [ ] GitHub OAuth works on production
- [ ] /explore shows seeded APIs on production
EOF
)"
```

---

## Self-Review

**Spec coverage check:**
- Browse APIs → Task 5 (explore page)
- Live sandboxed demos → Task 6 + 7 (sandbox + detail page)
- Auth → Task 3
- Bookmarks → Task 8
- Collections → Task 8
- Community demo submission → Task 9
- Upvotes → Task 9
- Comments → Task 9 (partial — addComment action exists, CommentThread UI is follow-up)
- Visual polish / hero → Task 10
- Profile → Task 11
- GitHub repo → Task 1
- Deploy → Task 12

**Gaps:**
- CommentThread real-time UI: add as follow-up task after Task 9 PR merges
- API key manager: not in scope for v1
- User-submitted sandbox code execution: requires server-side runner for security (future task)

**No placeholders found.** All steps contain real code or exact commands.

**Type consistency:** `PublicApi` type defined in Task 4, used in seed. DB row type inferred by Supabase client (no manual type needed for v1).
